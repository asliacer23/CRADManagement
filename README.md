# CRAD Management System

## Default accounts

Use these local demo accounts for testing:

- Admin: `admin@gmail.com` / `admin123`
- Staff: `staff@gmail.com` / `admin123`

These map to the seeded CRAD demo users in Supabase, including `admin.seed@crad.local` and `staff.seed@crad.local`.
